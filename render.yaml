services:
  - type: web
    name: xynex
    env: node
    plan: free
    buildCommand: npm install && npm install --prefix server && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 24
